# Provider presets

These files contain only the model selection for common providers.

The configuration scripts merge the selected model identifiers into the
workspace `opencode.jsonc`.

Model catalogs change over time. Authenticate the provider with `/connect`,
inspect `/models`, and update the selected identifiers when necessary.

API keys are never stored in these files.
