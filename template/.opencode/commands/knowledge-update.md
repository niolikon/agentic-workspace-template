---
description: Deeply reconcile existing repository knowledge with current code
agent: knowledge
subtask: false
---

Repository and optional knowledge aspect supplied by the user:

$ARGUMENTS

Execute the repository knowledge reconciliation workflow defined by
`knowledge-reconciliation`.

Load `knowledge-reconciliation` before starting and let that skill select and
compose the shared analysis, evidence, persistence and artifact-refresh
capabilities required by the resolved update concern.

The command owns only update intent and its repository/aspect arguments. Do not
reimplement reconciliation procedures in this command prompt.
