# Dependency analysis

Use:

```text
/repository-dependencies
```

For runtime integration analysis:

```text
Load the multi-repository-analysis skill and identify runtime integrations
between repositories. Separate confirmed integrations from probable ones and
cite configuration, client or API-definition files as evidence.
```
