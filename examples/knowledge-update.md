# Knowledge-base update

Use:

```text
/update-knowledge authentication and authorization
```

The Knowledge agent should update only affected files under `knowledge-base/`
and list the source files used.
