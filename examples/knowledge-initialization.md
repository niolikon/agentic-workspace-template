# Knowledge-base initialization

Use:

```text
/init-knowledge
```

The Knowledge agent inventories the available project sources and creates an
initial, traceable knowledge base under `knowledge-base/`.

Review the generated files, especially:

- source references;
- inferred relationships;
- conflicts between sources;
- missing or unverifiable information.
